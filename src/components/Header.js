import React from 'react';

const Header = ({ handleToggleDarkMode }) => {
    return (
        <div className='header'>
            <h1>Keep It Noted</h1>
        </div>
    );
};

export default Header;